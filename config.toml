[server]
listen_addr = "127.0.0.1:8080"

[upstream]
url = "http://127.0.0.1:9002"

# Rules (authz, dlp, hitl) are pulled from the central dashboard.
# No static rules here — the dashboard is the single source of truth.

[breaker]
requests_per_second = 50
burst_size = 100

[audit]
enabled = true
sink = "stdout"

[heartbeat]
central_url = "https://hesed.astapa.com"
interval_secs = 30
api_key = "hsk_73f1afe079124202822a3e41ba3ca293"
